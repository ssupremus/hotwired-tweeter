<%= content_tag :div, class: "flex items-center bg-blue-400 border-l-4 py-2 px-3 shadow-md mb-2 #{'border-blue-700' unless notification.read_at?}" do %>
  <div class="text-white max-w-xs ">
    <%= link_to "@#{notification.user.username}", notification.user %> <%= notification.action %> you!
  </div>
<% end %>
