<%= form_with(model: tweet, id: dom_id(tweet)) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-8 w-screen max-w-xl flex flex-col">
    <% if tweet.persisted? %>
      <%= form.text_area :body, class: 'p-4 rounded-xl resize-none max-w-lg', rows: 3, placeholder: 'What do you want to say?' %>
      <%= button_to 'Cancel', @tweet, class: 'py-3 my-8 text-lg bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl text-white max-w-lg' %>
      <%= form.submit 'Tweet', class: 'py-3 my-8 text-lg bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl text-white max-w-lg' %>
    <% else %>
      <%= form.text_area :body, class: 'p-4 rounded-xl max-w-xl resize-none', rows: 3, placeholder: 'What do you want to say?' %>
      <%= form.submit 'Tweet', class: 'py-3 my-8 text-lg bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl text-white' %>
    <% end %>
  </div>
<% end %>
